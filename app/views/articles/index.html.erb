<h1>Wikinimous</h1>

<ul>
  <% @articles.each do |article| %>
    <li><%= link_to article.title, article_path(article) %> <%= article.content %> </li>
  <% end %>
</ul>

<%= link_to "+ Add Article", new_article_path %>
